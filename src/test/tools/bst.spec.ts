import { should } from 'chai';

import { BST } from '../../olga/tools/bst';

should();

describe('Olga BST should', () => {

    it('make a BST from a single value', () => {
        const bst: BST = new BST(1);
    });
});
